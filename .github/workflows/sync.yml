name: Auto Sync Dev

on:
  schedule:
    - cron: '0 0 * * *' # Runs every day
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: gh repo sync $GITHUB_REPOSITORY -b dev
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
